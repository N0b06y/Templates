# Standart legend
```cpp
/**
 * @brief This is a sample app, with a text field and a button.
    * The button triggers a function @sendString and shows a snackbar.
 * @usage User Interface for sending inputs to FlightDeck-Metrics app to be used in MSFS
 * @functionalities BluetoothHID, Socket(TCP/IP), CompanionDeviceManager
 * @dependencies
 * @version 0.11
 * @date 2023
 * @author Matthes Neumann
 */
```